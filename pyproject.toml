[project]
name = "debertinha"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.4.1",
    "ftfy>=6.3.1",
    "pyarrow>=22.0.0",
    "sentencepiece>=0.2.1",
    "tokenizers>=0.22.1",
    "transformers[torch]>=4.57.1",
]
